exp_name: "mixup_1"
optim:
    name: "adam"
    lr: 0.0005
    wd: 0.000001
sched: "exp" #scheduler; either None or exp
pretrained: False #True or False
lr_scale: 10 # Used only for pretrained, in case you want to downscale backbone weight
transf: "basic" # None or  "basic"
batch_size: 1024
epochs: 100
mixup: 0.3

    